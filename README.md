# smartlib
Software Engineering project - self-service library app


HOW TO USE:

git pull

git switch -c [twoj_branch]     # zrobienie sobie brancha

git add .

git commit -m "[komentarz co zrobiliście"

git push origin [twoj_branch]
